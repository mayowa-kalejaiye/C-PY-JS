#include <stdio.h>

/* Strings
What are Strings?
Strings are arrays of characters terminated by a null character (\0).
*/
deep
int main() {
    char name[] = "Alice";

    printf("Name: %s\n", name);

    return 0;
}
